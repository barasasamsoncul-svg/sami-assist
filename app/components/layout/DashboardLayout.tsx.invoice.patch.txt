In app/components/layout/DashboardLayout.tsx:

1. Add this import with the other dashboard imports:
import Invoices from "../dashboard/invoices";

2. Find the current case "invoices" placeholder that says:
"Invoice management coming soon."

Replace that entire case with:
case "invoices":
  return (
    <div className="h-full overflow-y-auto p-5 sm:p-7 lg:p-9">
      <div className="mx-auto max-w-7xl">
        <Invoices />
      </div>
    </div>
  );

Do not create a second invoices case.
